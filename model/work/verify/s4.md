# Session 4 report (2026-09-23) — verification of the session-2/3 handover and new results

Working in the main tree (single agent, no swarm running).  Console runs: `eg_lock`, `feg_krs` (new cases only; no
existing capture was overwritten).  Everything C++ integer; outputs in `work/verify/s4/`.

## Handover claims (sessions 2 and 3)

| Claim | Verdict | Check |
|---|---|---|
| L1, L2 | CONFIRMED | `find` finds no ELF outside build/; `run_hw.sh` builds with `-DMODEL_ROOT`; captures of the new cases landed in this tree |
| F1–F3, F5 | CONFIRMED | `baseline.log`: filt_validate_model, filt_frac, filt_voff, lowrange identical to `expected/` |
| F4 (withdrawn) / F6 | CONFIRMED | filt_overflow (+model) 15/15 and 12/12 identical to `expected/` |
| F7 | CONFIRMED, bound refined | filt_reach2: mid-drive switch reaches band 5.32M / low 4.79M (> the 4.29M reported) but < 2^23 |
| E1–E3 | CONFIRMED (superseded) | feg_track / feg_validate identical to expected; feg_validate rewritten for the ring lock, still 9/9; eg_model 9/9 |
| E4 | RESOLVED | rows 5/9/13 (double step at index 1, 5) + a key-off clock stepping with the previous increment; no per-slot timing; KRS irrelevant (feg_krs) |
| A1 | CONFIRMED, refined | aeg_dl0 exact through eg_model; the DL compare is `a[9:5] == DL` (sgc_loop lo_2 LPSLNK); R 63 attack leaves the attack on the next clock |
| A2, A3 | CONFIRMED | levels unchanged after alignment; monitor timing as stated |
| D1 | not re-checked (out of scope) | — |
| D2 | RESOLVED | MIXS retention on unsent buses + two DSP read banks (eg_lock mixs) |
| D3 | not re-checked | — |
| "key events take effect at the next envelope clock" (sgc_aeg, session 1) | REFUTED | eg_lock keys: next sample, any parity (key-on level 1 or 2 samples) |
| "AEG counter offset unmeasured" | RESOLVED | the AEG needs the same -1 offset (same K as the FEG) |

## New results

See NOTES.md "Envelope clock, key timing and increment rows" and "MIXS retention"; HANDOVER.md S1–S9 lists the
checks and expected outputs.  Headline: `build/tools/eg_model` → `TOTAL full=33/33`.

## Files

Cases `cases/eg_lock.c`, `cases/feg_krs.c` (+ `tests/eg_lock/{hw,model}`, `tests/feg_krs/{hw,model}`); tools
`eg_model`, `eg_phase`, `eg_keys`, `feg_lock`, `filt_negform`, `filt_reach2`, `feg_validate` (rewritten); model
`src/aica_model.{h,cpp}`; scratch `work/eg/`; outputs `work/verify/s4/`, expected `work/verify/expected/`;
hashes `work/model_outputs_2026-09-23b.sha256`.
